# temporal-demo

## Start Temporal

```bash
docker-compose up -d temporal-server temporal-web
```

- server port: `7233`
- web port: `10000`

## Start the API

```bash
docker-compose up -d api
```

- port: `8080`
